import React from 'react';

const LanguageCard = ({ language }) => {
  return (
    <div>
      <h3>{language.name}</h3>
      {/* Add other details about the language as needed */}
    </div>
  );
};

export default LanguageCard;
